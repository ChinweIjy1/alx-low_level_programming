#include "main.h"
/**
 * islower - checks for lowercase
 * Description: c is an ascii character
 * Return: 1 if lowercase
*/
int _islower
for (i = 'a'; i <= 'z'; i++)
{
if (c == i)
{
return (1);
}
}
return (0);
